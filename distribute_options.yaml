output: dist/

releases:
  - name: linux-rpm
    jobs:
      - name: linux-rpm
        package:
          platform: linux
          target: rpm

  - name: linux-deb
    jobs:
      - name: linux-deb
        package:
          platform: linux
          target: deb

  - name: linux-appimage
    jobs:
      - name: linux-appimage
        package:
          platform: linux
          target: appimage

  - name: windows-msix
    jobs:
      - name: windows-msix
        package:
          platform: windows
          target: msix

  - name: macos-zip
    jobs:
      - name: macos-zip
        package:
          platform: macos
          target: zip